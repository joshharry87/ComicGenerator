import numpy as np

# 

def create_hair_mapping(array, perspective='f'):
    """
        Generates numpy array mapping behaviour for each x y pixel.
        (Colour gradient, movement etc.)
    Args:
        Input array 
        Perspective

    Returns:
        
    """
    
    
    
    return array



def create_head_mapping(array, perspective='f'):
    """
        Generates numpy array mapping behaviour for each x y pixel.
        (Colour gradient, movement etc.)
    Args:
        Input array 
        Perspective

    Returns:
        
    """
    
    return array

def create_eye_mapping(array, perspective='f'):
    """
        Generates numpy array mapping behaviour for each x y pixel.
        (Colour gradient, movement etc.)
    Args:
        Input array 
        Perspective

    Returns:
        
    """
    
    return array


def create_nose_mapping(array, perspective='f'):
    """
        Generates numpy array mapping behaviour for each x y pixel.
        (Colour gradient, movement etc.)
    Args:
        Input array 
        Perspective

    Returns:
        
    """
    
    return array



def create_mouth_mapping(array, perspective='f'):
    """
        Generates numpy array mapping behaviour for each x y pixel.
        (Colour gradient, movement etc.)
    Args:
        Input array 
        Perspective

    Returns:
        
    """
    
    return array



